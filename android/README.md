# Android Client

Planned Android tablet client for Loom.

Expected responsibilities:

- receive encoded video over USB transport
- decode with MediaCodec
- display fullscreen with low latency
- send input/control messages back to `loomd`

