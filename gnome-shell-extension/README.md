# GNOME Shell Extension

Planned GNOME Shell UI for Loom.

Expected responsibilities:

- show `loomd` connection/display status
- enable or disable the virtual display
- expose tablet pairing and streaming controls
- call into `loomctl` or the daemon control protocol

