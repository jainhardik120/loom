# loomctl

Planned command-line client for controlling `loomd`.

Expected responsibilities:

- query daemon status
- attach/detach virtual display
- select render node and encoder settings
- expose diagnostics for GNOME extension and scripts

